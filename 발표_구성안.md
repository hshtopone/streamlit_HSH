# 정적 VI 변동폭 최적화 — 발표 구성안 (15분)

> **주제**: 정적 VI 발동 변동폭의 최적값 탐색 — ARENA 이산사건 시뮬레이션
> **분량**: 15분 / 총 17슬라이드 / 슬라이드당 평균 ~50초
> **★ = 모듈·구현 핵심 파트 (약 5분 배분)**

---

## ① 표지 (0:15)
- **제목**: 정적 VI 발동 변동폭의 최적값 탐색 — ARENA 이산사건 시뮬레이션
- 부제: 가격안정성·가격발견·거래마찰의 통합 평가
- 발표자 / 소속 / 날짜

## ② 연구 배경 (1:00)
- KRX 정적 VI 제도: 2015.6 도입, **직전 단일가 체결가 대비 ±10% 변동 예상 시 2분 정지** 후 단일가 재개
- 선행연구(안일찬 외, 2017)의 발견:
  - 정적 VI의 가격안정화 효과가 낮음
  - 상한가일의 88%에서 가격제한폭과 중복 발동
  - **"±10% 등 모수에 대한 심도 깊은 검증 필요"** ← 후속 연구 요청
- **말할 포인트**: "이 발표는 그 후속 요청에 직접 답한다"

## ③ 연구 목적·문제 정의 (1:00)
- 핵심 질문: **VI 발동 변동폭 w를 5~15%로 바꾸면 시장 품질은 어떻게 달라지는가? 최적값은?**
- 평가 3축(모두 작을수록 좋은 손실지표):
  - 가격불안정성 S / 가격발견 지연 D / 거래마찰 비용 C
- **말할 포인트**: 단일 지표가 아닌 다목적 최적화 문제임을 강조

## ④ 모델 개요·가정 (1:00)
- 단기 상승국면: 시가 100원, 적정가 125원, 호가단위 1원, 상한가 130원
- 적정가 > 시가, 적정가 ≤ 상한가 / 동적 VI·임의종료 무시 / VI 정지 정확히 2분
- 종료조건: 적정가 ±5%(119~130원) 밴드에 **접속매매 중 2분간 유지**
- **말할 포인트**: 왜 상승국면 단순화가 합리적인지 한 줄 변론

---

## ⑤ ★ 전체 모듈 아키텍처 (1:00)
- 4개의 독립 엔티티 흐름을 그림으로:
  1. **매수 주문 생성** 라인
  2. **매도 주문 생성** 라인
  3. **VI 제어(단일가매매)** 라인
  4. **감시(종료조건·지표수집)** 엔티티
- **말할 포인트**: "주문 엔티티와 감시 엔티티를 완전히 분리한 것이 설계의 뼈대"

## ⑥ ★ 주문 생성 모델 — 상태의존적 포아송 (1:00)
- 매 1초 잠재 주문 생성 → **gap(적정가−현재가)에 따라 통과확률 차등 부여**
- gap 클수록 매수압력↑·매도압력↓ (piecewise)
- ARENA 구현: `Create → Assign(A_GenProb) → Decide(2-way by Chance, A_GenProb×100)`
- **누적 차분 수식**으로 `<` 연산자 회피 (예: `(gap≥20)*(1/1.5−1/2.0)+…`)
- **말할 포인트**: 시장 미시구조 동학을 확률로 모델링

## ⑦ ★ 호가창 = 배열변수 + 압축 탐색 수식 (1:00)
- `BidQty[51]`, `AskQty[51]` (가격 80~130, 인덱스 = 가격−79)
- 최우선호가 탐색을 **MN/MX에 51개 인자를 한 번에 주입**하는 단일 Assign으로 압축
  - `V_BestAsk = MN((AskQty(i)>0)*price + (AskQty(i)==0)*9999, …)`
- **효과**: 31 Decide + 31 Assign(60+ 모듈) → **2개로 축소** (학생용 모듈 한도 극복)
- **말할 포인트**: 이 발표의 가장 큰 구현 기여

## ⑧ ★ 체결 로직 — Label/Go-to 반복 매칭 (1:00)
- 연속매매: 주문가 ≥ BestAsk면 체결 → 잔량 남으면 **Go to Label로 다음 호가와 재매칭**
- ARENA에 반복문이 없어 **Label 메커니즘으로 루프 구현**
- 체결 시 순차 Assign으로 잔량 차감·가격 갱신·지표 누적
- **말할 포인트**: "명시적 for문 없는 도구에서 부분체결 반복을 어떻게 구현했는가"

## ⑨ ★ VI 제어 & 단일가매매 (1:15)
- VI 발동 검사 → **Separate로 제어 엔티티 1개 복제** → 120초 Delay → 단일가매매
- **단일가 결정(개선판)**: 교차분을 먼저 전부 반복 체결 → 마지막 체결 한계가로 단일가 사후 결정
  - `V_CallPrice = MAX(LastAsk, MIN(V_RefPrice, LastBid))` (기준가 우선 = 실제 거래소 규칙)
- **말할 포인트**: 시간지연과 상태전이를 분리한 표준 패턴 + 최대거래량 보장

## ⑩ ★ 감시 엔티티 — 종료조건 & 지표수집 (0:45)
- 매 1초 독립 엔티티가 밴드·접속매매 여부 점검, 2분 유지 시 종료
- 같은 엔티티가 **S/D/C 원자료를 초당 누적**(제곱편차·시간가중 미체결량)
- NREP로 `V_VIWidth` 자동 변경 → **단일 Run으로 11×30=330 데이터**
- **말할 포인트**: 메타 레벨 자동 실험 설계

---

## ⑪ 구현 중 만난 이슈와 해결 (1:15)
- 표로 압축 제시:
  - 스칼라 변수 Rows 비우기 (배열 인식 에러)
  - 모듈 수 한도 → 압축 수식
  - 배열 인덱스 초과 → 31→51 확장
  - `<`, `!=` 미지원 → 누적차분 / `1−(==)` 우회
  - Base Time Units = Seconds 필수
- **말할 포인트**: "도구 제약과의 싸움이 곧 모델링 역량" — 1~2개만 구두 설명

## ⑫ 모델 검증 — 자체 오류 검토와 수정 (1:15)
- 검토에서 발견한 3가지 + 수정:
  1. **단일가 체결**: 단일가 한 칸만 체결 → **교차분 전체 청산**으로 수정
  2. **안정시간**: VI 정지 120초 오집계 → **접속매매 중에만 측정**(`V_VIMode==0`)
  3. **미체결량**: 양면 합산 오류 → **호가창 배열 직접 합산**(`ΣBid+ΣAsk`)
- **말할 포인트**: 결과의 신뢰성을 스스로 검증했다는 점이 강점

## ⑬ 평가 체계 — S / D / C와 종합점수 (1:15)
- S: 밴드 진입 후 접속매매 시점의 `((Pt−125)/125)²` 평균
- D: 지속적 가격발견 시점(VI 정지시간 포함한 실제 경과시간)
- C: 시간가중 미체결량 / (종료시간 × 총주문) — 거래마찰
- 후처리: 정규화 → 파레토 지배 제거 → **Augmented Tchebycheff** → 가중치 민감도
- **말할 포인트**: ARENA는 원자료만, MCDM은 엑셀/파이썬 후처리

## ⑭ 민감도 분석 설계 (1:00)
- 종료조건(band% × dwell)과 적정가는 **상한가 130을 통해 상호작용**
- 순수 곱(125)도 합(30)도 아닌 **표적 혼합**:
  - 종료조건 격자(band% × dwell) + band%×적정가 교차검증
  - 적정가는 {115,120,125}로 한정(135는 상한가 초과로 무효)
- 공통난수(CRN)로 공정 비교
- **말할 포인트**: 무효 셀과 상호작용을 인지한 설계

## ⑮ 결과 (1) — 변동폭별 패턴 (1:00)
- 표/그래프: w별 S, D, C, 발견실패율
- 핵심 관찰:
  - 모든 w에서 VI 1회 발동
  - **w=6%가 오버슈팅 임계점**(6.7%→66.7% 급증)
  - w↓: 안정성·유동성↑·발견시간↑ / w↑: VI 사실상 무력화
- **말할 포인트**: 그래프 한두 개로 추세를 보여주기

## ⑯ 결과 (2) — 종합점수 & 민감도 강건성 (1:00)
- 동일가중 Tchebycheff 종합점수 순위 + 가중치 시나리오별 최적 w
- 민감도(band%/dwell/적정가)에서 최적 w가 얼마나 유지되는지
- **잠정 결론**: 현행 ±10%보다 **낮은 구간(5~7%)이 후보**
- **말할 포인트**: "결론이 가중치·기준 변화에도 강건한가"가 진짜 메시지

## ⑰ 결론·한계·향후 (1:00)
- 결론: 최적 변동폭 = 파레토 비지배 + 종합점수 우수 + 가중치 강건성 충족하는 값
- 한계: 단일 종목·상승국면만·포아송 가정·시가 고정(배열 제약)
- 향후: Hawkes 과정, 하락국면, 다종목 상호작용
- **말할 포인트**: 선행연구 요청에 답했고, 후속 방향까지 제시

---

## 시간 배분 요약

| 파트 | 슬라이드 | 시간 |
|---|---|---|
| 배경·목적·모델 | ②~④ | ~3:00 |
| **★ 모듈·구현** | ⑤~⑩ | **~5:00** |
| 이슈·검증·평가 | ⑪~⑬ | ~3:45 |
| 민감도·결과·결론 | ⑭~⑰ | ~4:00 |

---

## 발표 팁
- ⑤(아키텍처 그림)와 ⑦(압축 수식)은 **시각자료에 가장 공들일 슬라이드** — 청중이 "어떻게 구현했나"를 여기서 이해합니다.
- ⑪·⑫는 **빠르게 표로 훑되**, 인상적인 1~2개(압축 수식, 단일가 수정)만 구두 강조.
- 결과 슬라이드는 숫자표보다 **그래프 1~2개**로.
