# ARENA 실시간 주가·VI 애니메이션 구현 가이드

> **목표**: 시뮬레이션이 돌아가는 동안 ① 주가가 실시간으로 변하는 차트, ② VI(거래정지)가 발동되는 모습을
> ARENA 화면 안에서 눈으로 보면서 확인할 수 있게 만든다.
>
> **대상 모델**: 정적 VI 시뮬레이션 (`ARENA_정적VI_시뮬레이션_전체정리.md` 참조)
> **기반 자료**: 4장(Model 4-3 애니메이션), 팁 PDF(3-2 Plot, 4-3 Variable, 5-1 Store/Unstore, 5-4 Level)

---

## 핵심 아이디어 (먼저 큰 그림)

당신 모델은 이미 **숫자(로직)는 완성**되어 있다. 애니메이션은 **로직을 건드리지 않고**
화면에 "보여주는 장치"만 얹는 작업이다. (팁 4-3의 *논리와 애니메이션 분리* 원칙)

만들 대시보드는 5개 블록:

```
┌─────────────────────────────────────────────────────────┐
│  ① 실시간 주가 차트 (Plot)                                │
│     - 현재가 / 적정가(125) / VI기준가 / VI상한선          │
├──────────────────┬──────────────────────────────────────┤
│ ② 숫자판          │  ③ VI 거래정지 박스 (Storage)         │
│   현재가          │     VI 발동 시 개체가 120초간 머무름   │
│   VI폭/VI상태     │                                       │
│   체결수/VI횟수   │  ④ 시뮬레이션 시계 (Clock, TNOW)      │
├──────────────────┴──────────────────────────────────────┤
│  ⑤ (선택) 호가창 사다리 (Level 막대들)                    │
└─────────────────────────────────────────────────────────┘
```

가장 중요한 건 **①주가 차트**와 **③VI 거래정지 박스** 두 개다. 나머지는 보강.

---

## 0단계 — 애니메이션을 켜기 위한 준비 (Run Setup)

애니메이션을 "볼 수 있게" 먼저 설정부터 바꾼다.

1. 상단 메뉴 **Run > Setup > Replication Parameters**
   - **Number of Replications: `1`** ← 여러 번 돌리면 화면이 휙휙 지나감. 우선 1회만.
   - Base Time Units: **Seconds** (이미 그렇게 되어 있어야 함. 아니면 애니메이션이 안 돎)
2. **Run > Run Control** 메뉴에서 **`Batch Run (No Animation)` 체크 해제** 확인
   - (팁에서 속도 위해 켜라고 했던 그 옵션 — 지금은 **꺼야** 그림이 보인다)
3. VI폭을 특정 값으로 고정해 보고 싶으면:
   - 헤더용 개체의 `Assign_SetVIWidth` 가 `V_VIWidth = 4 + NREP` 로 되어 있는데,
     Replication이 1이면 NREP=1 → **V_VIWidth=5**가 된다.
   - 다른 값(예: 9)을 보고 싶으면 이 Assign을 잠시 `V_VIWidth = 9` 로 바꿔서 실행.
     (실험 돌릴 땐 원래대로 복구)

> ✅ 체크: 이 상태로 한 번 실행(▶)해서 기본 큐/개체 이동(Spartan 애니메이션)이 보이면 준비 끝.

---

## 1단계 — 화면에 애니메이션 영역 만들기

복잡한 로직 모듈들과 그림이 겹치지 않게 **빈 공간을 확보**한다. (4장 38p)

1. 모델 캔버스에서 로직 모듈들이 모여 있는 곳의 **오른쪽 빈 영역**으로 스크롤.
2. (선택) **View > Named Views** 로 "애니메이션" 뷰를 하나 저장해두면 로직↔대시보드 전환이 편하다.
3. 아래 모든 애니메이션 객체는 이 빈 영역에 배치한다.

> 💡 Animate 툴바가 안 보이면: **View > Toolbars > Animate** 체크.
> 이동 애니메이션(Station/Storage)은 **Animate Transfer** 툴바.

---

## 2단계 — ⭐ 실시간 주가 차트 (Plot) ⭐  [가장 중요]

주식 차트처럼 시간에 따라 현재가가 움직이는 그래프. (4장 42p, 팁 3-2)

### 2-1. Plot 객체 놓기
1. **Animate 툴바 > Plot 버튼** 클릭.
2. 빈 영역에서 **드래그**하여 직사각형(차트 크기)을 그린다. → Plot 설정창이 뜸.

### 2-2. 데이터 시리즈 추가 (Data Series 탭)
**Add** 버튼으로 아래 4개 시리즈를 차례로 만든다. 각 시리즈는
`Name`(이름) 과 `Expression`(그릴 수식) 을 지정한다.

| # | Name | Expression | Draw Mode | 선 색/굵기 |
|---|---|---|---|---|
| 1 | 현재가 | `V_CurrentPrice` | **Stairs**(계단) | 파랑, 굵게 |
| 2 | 적정가 | `V_FundamentalPrice` | PointToPoint | 초록 점선 |
| 3 | VI기준가 | `V_RefPrice` | **Stairs** | 회색 |
| 4 | VI상한선 | `V_RefPrice * (1 + V_VIWidth/100)` | **Stairs** | 빨강 |

- **현재가**가 주인공(실제 체결가 변동). Stairs로 해야 체결 순간 "툭툭" 점프하는 진짜 주가 모양.
- **적정가(125)**는 평평한 목표선.
- **VI상한선**은 "이 빨간 선에 현재가가 닿으면 VI가 터진다"를 한눈에 보여주는 선. → 교육 효과 최고.
- Expression 칸은 우클릭 → **Build Expression**(Expression Builder)으로 변수 골라 넣어도 된다.

### 2-3. 축 범위 설정 (Axes 탭)
- **Time (X축, 가로=시간)**:
  - Scale `Minimum: 0`, `Maximum: 720` (대략 한 번 실행 길이. 보통 300~700초에 종료되니 720이면 충분)
  - `Time Range`(한 화면에 보여줄 시간폭): `720` 으로 두면 전체가 한 화면에. 흐르는 차트를 원하면 `120` 같이 작게.
- **Left Value (Y축, 세로=가격)**:
  - Scale `Minimum: 95`, `Maximum: 132`
  - (시가 100, 적정가 125, 상한가 130 범위가 다 들어오게)
- **OK**.

> ✅ 체크: 실행하면 파란 현재가 선이 100 근처에서 시작해 빨간 VI상한선을 향해 올라가다가,
> 닿는 순간 VI가 발동(다음 단계에서 시각화)하고, 단일가매매 후 한 번에 점프하는 모습이 보인다.

---

## 3단계 — 실시간 숫자판 (Variable & Clock)

차트 옆에 핵심 숫자들을 띄운다. (4장 42p, 팁 4-3)

### 3-1. 숫자 표시 (Variable 객체, X| 아이콘)
**Animate 툴바 > Variable 버튼** → 화면 클릭 → 더블클릭하여 `Expression` 입력.
아래를 각각 하나씩 만든다. 옆에 **Text 도구(A)**로 라벨도 붙인다.

| 라벨(Text로 직접 입력) | Variable Expression | 비고 |
|---|---|---|
| `현재가:` | `V_CurrentPrice` | 정수 표시(소수 0자리) |
| `적정가:` | `V_FundamentalPrice` | 125 고정 |
| `VI 폭(%):` | `V_VIWidth` | 지금 보는 시나리오 |
| `VI 상태:` | `V_VIMode` | **0=정상 / 1=정지** |
| `VI 발동수:` | `V_NumVI` | |
| `총 체결건수:` | `V_TotalTrades` | |
| `총 체결량:` | `V_TotalTradeVolume` | |
| `BestBid / BestAsk:` | `V_BestBid` , `V_BestAsk` | 2개 따로 |

- Variable 더블클릭 창에서 **Format**으로 소수 자릿수를 `0`(정수)으로 맞추면 깔끔하다.
- `VI 상태:` 옆에는 Text로 `(0=정상, 1=거래정지)` 라고 적어두면 보는 사람이 바로 이해.

### 3-2. 시뮬레이션 시계 (Clock)
**Animate 툴바 > Clock 버튼** → 화면 클릭. TNOW(현재 시각, 초)가 표시됨.
- "지금 몇 초째인지" + 종료 시점(EndTime) 감 잡기에 좋다.

---

## 4단계 — ⭐ VI 거래정지 시각화 (Storage) ⭐  [핵심 2]

"VI가 발동했다"를 가장 직관적으로 보여주는 방법:
**VI 발동 시 만들어지는 제어 개체가, 거래정지 120초 동안 박스 안에 머무는 모습**을 보여준다.
(팁 5-1의 *Store/Unstore* 기법을 그대로 활용)

당신 모델의 VI 제어 라인은 이미 이렇게 되어 있다:
```
[Separate_VI] ──복제──→ [Delay_VIHalt: 120초] ──→ [단일가 계산/체결] ──→ [Dispose]
```
여기서 **Delay 앞뒤를 Store/Unstore로 감싸면**, 그 120초 동안 개체가 "Storage(가상 보관소)"에
들어가 있고, 그걸 화면에 박스로 그릴 수 있다.

### 4-1. Store / Unstore 모듈 추가
1. 좌측 패널에서 **Blocks** 패널을 붙인다 (없으면: 패널 영역 우클릭 > **Attach** > `Blocks.tpo`).
2. **Store 모듈**을 `Delay_VIHalt` **앞**에 끼워 넣는다.
   - Storage Name: `VI_정지중` (새 이름 입력)
3. **Unstore 모듈**을 `Delay_VIHalt` **뒤**에 끼워 넣는다.
   - 연결: `Separate(복제)` → **Store** → `Delay_VIHalt` → **Unstore** → `단일가 계산...`
   - (매수/매도 양쪽 VI 라인이 따로 있으면, 둘 다 같은 Storage `VI_정지중`을 쓰면 된다)

> ℹ️ 대안: 일부 ARENA 버전은 **Delay 모듈(Advanced Process)** 자체에 Storage를 지정할 수 있다.
> 하지만 당신이 배운 방식(Store/Unstore)이 가장 확실하니 그대로 가자.

### 4-2. Storage 애니메이션 박스 그리기
1. **Animate Transfer 툴바 > Storage 버튼** 클릭.
2. **Identifier** 드롭다운에서 `VI_정지중` 선택.
3. 화면(③ 위치)에 드래그하여 박스(개체가 늘어설 자리)를 그린다.
4. 옆에 Text로 크게 **`⏸ VI 거래정지 중`** 라벨을 단다.

> ✅ 체크: 현재가(파란선)가 VI상한선(빨간선)에 닿는 순간 →
> 박스 안에 개체 1개가 **120초 동안** 나타났다가 → 사라지면서 주가가 점프한다.
> 이게 "VI 발동 → 2분 정지 → 단일가매매 재개"의 시각화다.

### 4-3. (강추) VI 발동 순간 강조
- `VI 상태:` Variable(`V_VIMode`)가 정지 중엔 1로 바뀐다. 그 옆에 빨간 **사각형 Box(도형)**를 하나 그려두고,
  Text로 `■ HALT` 라고 써두면, 박스에 개체가 들어찬 동안 시선이 간다.
- 더 화려하게: 4단계에서 VI 제어 개체에 **빨간 그림**을 입히면(아래 6단계) 박스 안 개체가 빨갛게 보인다.

---

## 5단계 — (선택) 호가창 사다리 (Level / 온도계)

매수·매도 잔량이 가격대별로 쌓이는 "호가창 깊이"를 막대로 보여준다. (4장 42p, 팁 5-4)
모든 51개를 그리면 너무 많으니, **적정가 주변 가격대만** 사다리로 만든다.

### 5-1. 한 가격대 막대 만들기 (Level 객체)
**Animate 툴바 > Level 버튼** → 화면 클릭 → 더블클릭:
- Expression: `AskQty(46)`  (가격 125원의 매도잔량. 인덱스 = 가격 − 79)
- Min `0`, Max `10` (한 가격대 잔량 상한 가늠치)
- Fill Color: 빨강(매도), Direction: Up
- 막대 위에 Text로 가격 `125` 표시

### 5-2. 가격 사다리로 복제
위 막대를 **복사(Ctrl+드래그)**해서 가격 119~130을 세로로 늘어놓는다.
각 막대의 Expression 인덱스만 바꾼다 (`인덱스 = 가격 − 79`):

| 가격 | 매도(빨강) Expression | 매수(파랑) Expression |
|---|---|---|
| 130 | `AskQty(51)` | `BidQty(51)` |
| 129 | `AskQty(50)` | `BidQty(50)` |
| ... | ... | ... |
| 125 | `AskQty(46)` | `BidQty(46)` |
| ... | ... | ... |
| 119 | `AskQty(40)` | `BidQty(40)` |

- 매도(Ask)는 빨강, 매수(Bid)는 파랑으로 좌우 또는 상하로 나란히 배치 → 실제 호가창처럼 보인다.
- 너무 많으면 **121~129** 정도(9칸)만 해도 충분히 그럴듯하다.

> ⚠️ 학생용 ARENA는 객체 수 제한이 있다. 사다리는 욕심부리지 말고 핵심 구간만.

---

## 6단계 — (선택) 개체 그림 구분 (Entity Pictures)

매수/매도/VI 개체를 다른 그림으로 보이게 한다. (4장 40p, 팁 4-3·4-1)

1. 상단 메뉴 **Edit > Entity Pictures**.
2. 왼쪽 목록에서 **Add** → 이름 `Pic_Buy` → 빈 버튼 더블클릭 → **초록 동그라미** 그리기(또는 라이브러리 복사).
3. 같은 방법으로 `Pic_Sell`(빨강 동그라미), `Pic_VI`(빨간 네모/⏸) 생성.
4. 개체에 그림 입히기 — **Assign 모듈**에서 한 줄 추가:
   - 매수 라인 `Assign_BuySide` 에: `Entity.Picture = "Pic_Buy"` (Type: Attribute, Other 아님 — 아래 주의)
   - 매도 라인 `Assign_SellSide` 에: `Entity.Picture = "Pic_Sell"`
   - VI 제어 라인(Separate 복제 직후 Assign) 에: `Entity.Picture = "Pic_VI"`

> ℹ️ ARENA에서 개체 그림은 `Entity.Picture` 라는 특수 속성에 **그림 이름 문자열**을 넣어 바꾼다.
> Assign 모듈 Add → Type: **Other**, Other 칸에 `Entity.Picture`, Value 칸에 `"Pic_Buy"` (따옴표 포함).

이렇게 하면 ③ VI 정지 박스 안의 개체가 빨갛게 보여서 효과가 확 산다.

---

## 7단계 — 실행 & 속도 조절

1. ▶ (Go) 로 실행.
2. **속도 조절**:
   - 키보드 **`>`** : 애니메이션 빠르게 / **`<`** : 느리게
   - 또는 **Run > Speed > Animation Speed Factor** 조정
   - 주가가 너무 빨리 지나가면 `<`로 늦춰서 VI 발동 순간을 관찰.
3. **일시정지**: `Esc` 또는 Run 메뉴 Pause. VI 발동 순간 멈춰서 캡처하기 좋다.
4. 한 단계씩 보고 싶으면 **Run > Step**(개체 하나씩 진행).

---

## 8단계 — 발표용 시연 시나리오 (추천)

1. **VI 폭 = 5** 로 실행 → VI가 자주 걸리고 주가가 빨간선에 자주 막히는 모습.
2. **VI 폭 = 9** 로 실행 → VI가 거의 안 걸리고 적정가(125)를 그냥 뚫고 올라가는(Overshoot) 모습.
3. 두 영상을 비교하면 "VI 폭이 작을수록 가격을 잘 잡아준다"는 예비 실험 결론(13장)이
   **그래프로 눈에 보인다.** → 발표 임팩트 ↑

---

## 체크리스트 / 자주 나는 오류

| 증상 | 원인 | 해결 |
|---|---|---|
| 애니메이션이 안 보이고 결과만 나옴 | Batch Run(No Animation) 켜짐 | Run Control에서 **해제** |
| 그래프가 안 움직이고 멈춤 | Base Time Units = Hours | **Seconds**로 변경 |
| 화면이 너무 빨리 지나감 | 속도 빠름 / Replication 많음 | `<` 키로 감속, Replication=1 |
| Plot 선이 안 보임 | Y축 범위 밖 | Y축 Min 95 / Max 132 확인 |
| Level 막대가 항상 꽉 참/빔 | Max 너무 작음/큼 | 한 가격대 Max 5~10로 조정 |
| Storage 박스에 개체 안 나타남 | Store/Unstore가 Delay를 안 감쌈 | 연결 순서 `Store→Delay→Unstore` 확인 |
| `Entity.Picture` 오류 | Type을 Attribute로 둠 | Type **Other**, 값에 `"이름"` 따옴표 |

---

## 우선순위 요약 (시간 없으면 이것만)

1. **2단계 주가 Plot** (현재가 + VI상한선 두 시리즈만이라도)
2. **4단계 VI 거래정지 Storage 박스**
3. **3단계 숫자판** (현재가, VI상태, VI발동수)

→ 이 3개만 해도 "실시간 주가 변동 + VI 발동"이 완벽히 시연된다.
나머지(호가창 사다리, 개체 그림)는 여유 있을 때 보강.

---

*작성: 2026-06 / 기반: 4장 Model 4-3·4-4, 팁 PDF 3-2·4-1·4-3·5-1·5-4*
